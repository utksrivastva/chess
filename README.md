# Terminal Chess

## Features

- ✅ ASCII / Unicode board rendering
- ✅ Move validation + turn enforcement
- ✅ Simple CLI commands (`move e2 e4`, `fen`, `help`, etc.)

## Getting Started

Prerequisites
	•	Node.js >= 18 (recommended: latest LTS)
	•	npm (bundled with Node) / pnpm / yarn
